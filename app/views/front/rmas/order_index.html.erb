<%= content_for :head_nav do %>
  <ul class="nav">
    <li><a href="<%= front_profile_path %>"><i class="icon-undo"></i></a></li>
    <li><a href="<%= order_index_front_rmas_path %>" id="recently">申请退货</a></li>
    <li><a href="<%= front_rmas_path %>" id="news">退货记录</a></li>
  </ul>
<% end %>
<div class="span9 main">
  <%= render 'front/message' %>
  <div class="Logistics_query">
    <% if @orders.present? %>
      <% @orders.each do |order| %>
        <%= render partial: 'front/orders/order', locals: { order: order } %>
      <% end %>
      <%= paginate @orders %>
    <% else %>
      <p style="text-align:center;font-size: 16px;line-height:30px;">
        暂无符合退货条件的订单！
      </p>
    <% end %>
  </div>
</div>
